spring:
  application:
    name: user-service
#  config:
#    import: optional:configserver:http://localhost:8888

  config:
    import: optional:configserver:https://fitness-config-server.onrender.com